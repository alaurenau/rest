apply plugin: 'java'
apply plugin: 'war'
apply from: 'gretty.plugin'

repositories {
    mavenCentral()
}

dependencies {
    providedCompile 'javax.servlet:servlet-api:2.5'
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.1'
}
